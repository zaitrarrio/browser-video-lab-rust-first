import {describe,expect,it} from 'vitest';import {ort} from './common';import {AdaptiveMemoryBank} from './memory-bank';
describe('adaptive MemFlow memory',()=>{it('retrieves prompt-relevant memories and stays bounded',()=>{const b=new AdaptiveMemoryBank(2,1),v=()=>new ort.Tensor('float32',new Float32Array([1]),[1,1]);b.add(new Float32Array([1,0]),v());b.add(new Float32Array([0,1]),v());expect(b.retrieve(new Float32Array([.9,.1]))[0].entry.key[0]).toBeCloseTo(1);b.add(new Float32Array([-1,0]),v());expect(b.size).toBe(2);expect(b.retrieve(new Float32Array([1,0]))[0].entry.key[0]).not.toBeCloseTo(1)});});
