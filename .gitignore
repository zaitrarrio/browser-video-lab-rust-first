# Node / Vite
node_modules/
dist/
*.local
*.tsbuildinfo
coverage/
npm-debug.log*
*.log

# Release artifacts (built by .github/workflows/release.yml)
release/

# Model build outputs (built by .github/workflows/models.yml)
public/rust-video/
model-bundle/
model-bundle.tar.gz

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# Rust
target/
**/*.rs.bk

# Artifacts / models / media
artifacts/
public/models/**/*.onnx
public/models/**/manifest.json
*.mp4

# Environment
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Build artifact: the orchestrator drops a compiled trainer here when it renders
# a kernel locally. 52 MB, reproducible from source, and it slipped into a commit
# once via `git add -A`.
bin/
